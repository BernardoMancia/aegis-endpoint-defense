services:
  aegis-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aegis-soc-server
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
